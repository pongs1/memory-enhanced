{
  "id": "memory-enhanced",
  "name": "Memory Enhanced",
  "description": "Clean-slate V8 memory graph built directly from raw OpenClaw session traces",
  "version": "8.0.1",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "outputCheckpointChars": {
        "type": "number",
        "default": 1600,
        "description": "Approximate output length threshold before the long-answer watchdog considers a checkpoint interrupt"
      },
      "outputCheckpointCooldownChars": {
        "type": "number",
        "default": 1000,
        "description": "Minimum output distance between checkpoint attempts"
      },
      "outputCheckpointBoundarySlackChars": {
        "type": "number",
        "default": 320,
        "description": "Extra slack allowed while waiting for a sentence or block boundary before interrupting"
      },
      "outputCheckpointMaxInterrupts": {
        "type": "number",
        "default": 2,
        "description": "Maximum number of output checkpoint interrupts allowed in a single reply"
      },
      "outputCheckpointDriftThreshold": {
        "type": "number",
        "default": 0.84,
        "description": "Drift score threshold that must be crossed before a soft-boundary checkpoint interrupt fires"
      },
      "outputCheckpointTailChars": {
        "type": "number",
        "default": 1400,
        "description": "Recent output tail length retained for drift scoring"
      },
      "enableV8GraphRecall": {
        "type": "boolean",
        "default": false,
        "description": "Enable graph-based recall when .memory/graph/graph_nodes.jsonl exists"
      },
      "v8CleanSlateMode": {
        "type": "boolean",
        "default": true,
        "description": "Use clean-slate V8 pipeline (raw session traces only, no legacy ingest)"
      },
      "v8SessionTraceDir": {
        "type": "string",
        "default": "/home/pongs/.openclaw/agents/main/sessions",
        "description": "OpenClaw session transcript directory"
      },
      "v8PackCacheTtlDays": {
        "type": "number",
        "default": 7,
        "description": "Default TTL (days) for cached summary/state packs"
      },
      "v8ScannerConfig": {
        "type": "object",
        "default": {},
        "additionalProperties": true,
        "description": "Optional overrides for V8 scanner thresholds (see V8ScannerConfig)"
      }
    }
  },
  "skills": ["skills"]
}

These are tracked copies of the workspace bootstrap templates from `default_md/`.

Use them as the canonical repo-backed versions when updating:

- `AGENTS.md`
- `HEARTBEAT.md`
- `USER.md`

The live workspace may still load templates from outside this repository, so changes here should be synced back to the active `default_md/` directory when needed.
